# ===== Django Core =====
# IMPORTANT: In production, generate a strong, random secret key.
SECRET_KEY=your-super-secret-django-key-change-me
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# ===== Site identity =====
# Used in confirmation/verification emails and SEO meta tags (canonical URL,
# Open Graph). Set to your real domain in production, e.g. fair.tergym.com.
SITE_NAME=FAIR
SITE_DOMAIN=127.0.0.1:8000
SITE_SCHEME=http

# ===== Database (SQLite for development) =====
DB_NAME=db.sqlite3

# ===== Celery / Redis =====
# Celery broker & result backend both use Redis (default localhost:6379/0).
REDIS_URL=redis://localhost:6379/0

# ===== Email / SMTP =====
# Development: console (prints emails to stdout). For REAL email set the
# backend to smtp and fill in the SMTP server details below.
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=reminders@yourdomain.com
EMAIL_HOST=smtp.yourprovider.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-smtp-username
EMAIL_HOST_PASSWORD=your-smtp-password
EMAIL_USE_TLS=True
# For port 465 use implicit SSL instead (this turns TLS off):
# EMAIL_USE_SSL=True

# ===== Twilio (WhatsApp reminders) =====
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_WHATSAPP_FROM=+15551234567
# NOTE: Each User also needs a `phone_number` (E.164 format) for WhatsApp.

# ===== Telegram bot (notifications) =====
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
# Public @username of the bot (no leading @) -- powers the one-tap "Connect
# Telegram" button on the profile page (t.me/<username>?start=<token>).
TELEGRAM_BOT_USERNAME=your_bot_username
# Random shared secret Telegram echoes back on every webhook call, so we can
# tell a genuine Telegram request apart from a spoofed POST to the same URL.
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Then register the webhook: python manage.py set_telegram_webhook
# (requires SITE_SCHEME=https, so this step is production-only).
TELEGRAM_WEBHOOK_SECRET=a-long-random-string

# ===== Reminder delivery =====
# Local hours (0-23) of the daily reminder slots, comma separated.
REMINDER_SEND_HOURS=9,14
# A missed slot is still sent on a cron run up to this many hours late.
REMINDER_GRACE_HOURS=3

# Timezone
TIME_ZONE=UTC
